# PyDoc dependencies
# --------------------------------------------------------------------------
# REQUIRED: the native window that renders the windows-ui (Fluent) front-end
pywebview>=4.0

# OPTIONAL bonus features (the app still runs without these):
keyboard      # global Alt+Space hotkey + per-shortcut hotkeys
pystray       # system-tray icon
pillow        # tray icon rendering (PIL)

# Quick install:  pip install -r requirements.txt
#
# Windows note: pywebview uses the built-in Edge WebView2 runtime
# (pre-installed on Windows 10/11). On Linux you may need:
#   pip install pywebview[qt]   (or [gtk])
#
# To build a standalone .exe (optional):
#   pip install pyinstaller && python build.py
